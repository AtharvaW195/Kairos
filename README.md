# Kairos

A full-stack trading platform simulator foundation.

## Run locally

1. Update `backend/.env` with your local values.
2. Run `docker compose up --build`.
3. Open `http://localhost:5173`; the API runs on `http://localhost:8000`.

The backend applies Alembic migrations before starting.
